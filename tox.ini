[tox]
envlist = lint, py3

[testenv]
deps = pytest
commands = pytest tests

[testenv:lint]
deps =
    flake8
    pylint
    pytest
commands =
    flake8 --ignore E501 smart_importer tests
    pylint smart_importer tests
